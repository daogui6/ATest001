/*
 * Category.ts - 文章分类数据模型接口
 * 
 * 功能说明：
 * - 定义文章分类的数据结构
 * - 用于文章分类管理和筛选
 * - 关联文章数据的cid字段
 * 
 * 数据字段说明：
 * - cid: 分类唯一标识
 * - name: 分类名称
 */

/**
 * 文章分类数据模型接口
 * 
 * 表示一个文章分类，用于对文章进行分类管理
 * 与Article接口的cid字段关联
 */
export interface Category {
  /**
   * 分类唯一标识符
   * 用于区分不同分类，与Article接口的cid字段对应
   */
  cid: number;

  /**
   * 分类名称
   * 显示给用户的分类名称
   * 如：技术、生活、娱乐等
   */
  name: string;
}